{
  "name": "onda-vscode",
  "displayName": "Onda",
  "description": "VSCode support for the Onda audio DSL",
  "version": "0.1.18",
  "publisher": "onda-lang",
  "icon": "assets/onda-logo-dark-rect.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vitreo12/onda.git"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [
    "onLanguage:onda",
    "onCommand:onda.restartLanguageServer",
    "onCommand:onda.runFile",
    "onCommand:onda.createProject",
    "onCommand:onda.saveAsProject",
    "onCommand:onda.stopFile"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "onda",
        "aliases": [
          "Onda",
          "onda"
        ],
        "extensions": [
          ".onda",
          ".on"
        ],
        "configuration": "./language-configuration.json"
      },
      {
        "id": "json",
        "extensions": [
          ".ondaproject"
        ]
      }
    ],
    "grammars": [
      {
        "language": "onda",
        "scopeName": "source.onda",
        "path": "./syntaxes/onda.tmLanguage.json"
      }
    ],
    "semanticTokenTypes": [
      {
        "id": "port",
        "superType": "variable",
        "description": "Onda audio input, output, or buffer symbol"
      },
      {
        "id": "state",
        "superType": "variable",
        "description": "Onda init-declared state symbol"
      },
      {
        "id": "delegate",
        "superType": "event",
        "description": "Onda output event declaration or call"
      }
    ],
    "semanticTokenScopes": [
      {
        "language": "onda",
        "scopes": {
          "enumMember": [
            "variable.other.constant"
          ],
          "variable": [
            "variable.other.readwrite"
          ],
          "port": [
            "support.variable"
          ],
          "state": [
            "variable.parameter"
          ],
          "parameter": [
            "variable.parameter"
          ],
          "function": [
            "entity.name.function"
          ],
          "event": [
            "entity.name.function"
          ],
          "delegate": [
            "entity.name.function"
          ]
        }
      }
    ],
    "commands": [
      {
        "command": "onda.runFile",
        "title": "Onda: Run",
        "icon": "$(play)"
      },
      {
        "command": "onda.createProject",
        "title": "Onda: Create Project…"
      },
      {
        "command": "onda.saveAsProject",
        "title": "Onda: Save as Project…",
        "icon": "$(save-as)"
      },
      {
        "command": "onda.stopFile",
        "title": "Onda: Stop"
      },
      {
        "command": "onda.restartLanguageServer",
        "title": "Onda: Restart Language Server"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "onda.runFile",
          "when": "resourceExtname == .onda || resourceExtname == .on || resourceExtname == .ondaproject",
          "group": "navigation@1"
        },
        {
          "command": "onda.saveAsProject",
          "when": "resourceExtname == .onda || resourceExtname == .on",
          "group": "navigation@2"
        }
      ],
      "explorer/context": [
        {
          "command": "onda.runFile",
          "when": "resourceExtname == .onda || resourceExtname == .on || resourceExtname == .ondaproject",
          "group": "navigation@1"
        },
        {
          "command": "onda.saveAsProject",
          "when": "resourceExtname == .onda || resourceExtname == .on",
          "group": "navigation@2"
        },
        {
          "command": "onda.createProject",
          "when": "explorerResourceIsFolder",
          "group": "navigation@3"
        }
      ]
    },
    "jsonValidation": [
      {
        "fileMatch": "*.ondaproject",
        "url": "./schemas/project.json"
      }
    ],
    "configuration": {
      "title": "Onda",
      "properties": {
        "onda.server.path": {
          "type": "string",
          "description": "Path to the Onda executable. Leave unset to use 'onda' from PATH."
        },
        "onda.server.args": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Additional arguments passed before the built-in 'lsp' subcommand."
        },
        "onda.run.theme": {
          "type": "string",
          "enum": [
            "auto",
            "dark",
            "light"
          ],
          "default": "auto",
          "description": "Theme used by the Onda run panel."
        },
        "onda.run.host": {
          "type": "string",
          "enum": [
            "webview",
            "egui"
          ],
          "default": "webview",
          "description": "Host used by Onda: Run. 'webview' opens the embedded VS Code panel; 'egui' opens the native egui run window."
        },
        "onda.run.sampleRate": {
          "type": "integer",
          "minimum": 1,
          "default": 48000,
          "description": "Sample rate in Hz used by Onda: Run."
        },
        "onda.run.blockSize": {
          "type": "integer",
          "minimum": 1,
          "default": 256,
          "description": "Compile block size in frames used by Onda: Run."
        }
      }
    }
  },
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('./out',{recursive:true,force:true})\"",
    "copy-run-ui": "node -e \"const fs=require('fs');fs.cpSync('./ui/run/run.html','./out/run.html');fs.cpSync('./ui/run/param-control.js','./out/param-control.js')\"",
    "compile": "npm run clean && tsc -p ./ && npm run copy-run-ui",
    "test": "npm run compile && node --test ./test/*.test.js",
    "vscode:prepublish": "npm run compile",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}
